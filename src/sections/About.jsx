export default function About() {
  return (
    <section id="about" className="page-section about reveal">
      <div className="section-header">
        <h2 className="section-heading">About the approach</h2>
        <p className="section-copy">
          The project structure separates content, behavior, and layout so future pages can grow without turning into a single giant file.
        </p>
      </div>
    </section>
  )
}
